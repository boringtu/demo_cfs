.root
	.content
		.logo
			i.icon.icon-logo
		form#loginBox
			el-input(v-model="username" placeholder="客服ID" :disabled="login_loading")
				.icon.icon-service(slot="prefix")
			el-input(v-model="password" placeholder="密码" :disabled="login_loading" type="password")
				.icon.icon-password(slot="prefix")
			el-button(type="primary" @click="eventSubmit" class="btn-submit" :loading="login_loading") 登录